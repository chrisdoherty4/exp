products:
- name: "Kendamil Goat Infant Powder Formula"
  stores:
  - type: target
    tcin: "88379079"
    store_id: "1113" # Coralville, IA
    zip: "52241"
  - type: walmart
    item_id: "5226743381"
    store_id: "2827"

polling:
  base_interval_seconds: 14400 # 4 hours
  jitter_factor: 0.5 # ±50% → checks every 7.5–22.5 min

notifications:
- type: log # stdout — always enabled
  log_level: debug

# - type: email              # requires SMTP_HOST, SMTP_FROM env vars
#   recipients:              # optional: SMTP_PORT (default 587), SMTP_USER, SMTP_PASSWORD
#     - you@example.com

status_page:
  enabled: true
  host: "127.0.0.1"
  port: 8080

# proxy:
#   url: "http://user:pass@gate.smartproxy.com:7777"
